build/
*.o
*.a
.vscode/
compile_commands.json
external/

# python_bindings/ build artifacts
*.so
__pycache__/
*.egg-info/
.pytest_cache/

# Sweep output. These are measurements, not build artefacts — they cannot be
# regenerated without the two-NIC setup — so the published dataset is tracked
# and only scratch runs are ignored.
*.csv
!results/*.csv
